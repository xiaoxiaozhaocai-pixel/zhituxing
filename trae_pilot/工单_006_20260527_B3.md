# 第六批 Trae 工单 B3（2026-05-27 晚）—— 前端剥离方案 C（合规必做）

> 主控：小扣 / 主人：肖赵才  
> 仓库：https://gitee.com/xiao-zhaos-legacy/zhituxing.git main  
> 前置：B1/B2 不阻塞此工单（独立工作流）  
> 截止：6/5 OPC 答辩前必须上线

---

## 🎯 大背景（必读，不读必做错）

今晚 19:24 已把 Coze 旧库 19997 条真 JD 入库 supabase，真实数据从 659 → **20656**，答辩硬指标 ≥20000 已达成。

但这些 JD 的 `source_url` 字段含 BOSS 直聘 / 实习僧 / 猎聘 / 智联 / 51job / 职友集 等商业平台的真链接（~4881 条）。这些链接：
- ✅ **数据库可以存**（合规追溯、未来正规化授权后切换源用）
- ❌ **C 端 API 绝对不能返回**（被任何人扒一下就知道我们爬过这些平台，法律风险）

> **法律风险铁律（主人 5/27 17:54 原话）**：商业招聘平台的真链接只能存数据库不能加到网页前端。

---

## ⚠️ 铁律

1. **不要碰** `src/lib/auth.ts` / `src/components/Navbar.tsx` / `src/lib/supabase*.ts`
2. **admin 路径必须保留完整字段**（`/api/admin/**` 一律跳过脱敏，用于后台调试）
3. **`pnpm run build` 必须 EXIT=0 才允许 push**
4. **1 任务 1 commit 1 push**
5. **不要 npm install 新依赖**

---

## 敏感字段清单（统一定义）

```typescript
// 必剥
const SENSITIVE_FIELDS = ['source_url', 'source_platform', 'raw_jd'] as const
```

- `source_url`：原始链接，会暴露平台来源
- `source_platform`：直接写着"BOSS直聘"等
- `raw_jd`：原始 JD 文本，可能夹带链接残留

---

## 任务 1（P0）：新建 `src/lib/jd-sanitizer.ts` 脱敏工具

### 文件
`src/lib/jd-sanitizer.ts`（新建）

### 内容要求

```typescript
/**
 * JD 对外脱敏工具
 * 用途：从 supabase 拉出的 JD 数据，在返回给 C 端用户前必须过一遍
 * admin 路径不应使用此工具（保留完整字段调试用）
 */

export const SENSITIVE_JD_FIELDS = ['source_url', 'source_platform', 'raw_jd'] as const

type AnyObj = Record<string, any>

/** 单条脱敏 */
export function sanitizeJD<T extends AnyObj>(jd: T | null | undefined): T | null {
  if (!jd) return null as T | null
  const clean: AnyObj = { ...jd }
  for (const f of SENSITIVE_JD_FIELDS) {
    if (f in clean) delete clean[f]
  }
  return clean as T
}

/** 批量脱敏 */
export function sanitizeJDList<T extends AnyObj>(list: T[] | null | undefined): T[] {
  if (!Array.isArray(list)) return []
  return list.map(jd => sanitizeJD(jd) as T)
}
```

### [ ] 验收
- [ ] 文件存在
- [ ] 导出 `SENSITIVE_JD_FIELDS`、`sanitizeJD`、`sanitizeJDList` 三个 API
- [ ] `pnpm run build` 通过

### Commit
```
feat(lib): 新建 jd-sanitizer 工具剥离 source_url/source_platform/raw_jd
```

---

## 任务 2（P0）：盘点所有需要剥离的 C 端 API

### 文件
`trae_pilot/B3_api_audit.md`（新建，盘点清单）

### 操作

```bash
# 1. 找出所有 select * 或 select 含敏感字段 的 API
grep -rln "job_descriptions" src/app/api/ | grep -v "/admin/"

# 2. 对每个文件，判断：
#    A. 是否对 C 端返回 JD 数据？（看响应里有没有 source_url）
#    B. 当前 .select() 是 * 还是白名单？
#    C. 是否已经手动 delete 敏感字段？
```

### 输出格式（在 B3_api_audit.md 里）

```markdown
| 文件 | C端? | 当前select | 当前是否含敏感字段 | 需改 |
|------|------|----------|--------|------|
| src/app/api/jobs/route.ts | ✅ | `select('*')` | 是 | ✅ |
| src/app/api/jd/recommend/route.ts | ✅ | `select(...)` | ... | ... |
| src/app/api/match/route.ts | ✅ | ... | ... | ... |
| src/app/api/admin/diagnostics/route.ts | ❌ admin | - | - | ❌ 跳过 |
...
```

### [ ] 验收
- [ ] 文件存在
- [ ] 18 个含 job_descriptions 的文件全部列出（admin 标记跳过）
- [ ] 每个文件都判断了 C端？/ 当前 select / 是否需改

### Commit
```
docs(trae): B3 API 盘点清单
```

---

## 任务 3（P0）：改造 C 端 API 加双保险（方案 A + B）

### 涉及文件（按 B3_api_audit.md 任务2结果）

至少包括但不限于：
- `src/app/api/jobs/route.ts`
- `src/app/api/jobs/list/route.ts`
- `src/app/api/jd/recommend/route.ts`
- `src/app/api/match/route.ts`
- `src/app/api/match/underrated/route.ts`
- `src/app/api/search-jd/route.ts`
- `src/app/api/interview/route.ts`
- `src/app/api/competency/route.ts`
- `src/app/api/career-planning/stream/route.ts`
- `src/app/api/chat/route.ts`
- `src/app/api/industries/route.ts`（如果返回 JD 数据）
- `src/app/api/filters/route.ts`（如果返回 JD 数据）

### 改造规范（每个文件）

**方案 A（数据库层白名单 select）**：把 `.select('*')` 改成显式列出非敏感字段（推荐写成多行常量提升可读性）

```typescript
// 在文件顶部定义
const PUBLIC_JD_FIELDS = [
  'id', 'job_title', 'company', 'industry', 'city',
  'salary_range', 'education', 'experience', 'post_nature',
  'responsibilities', 'hard_skills', 'soft_skills', 'tags',
  'fresh_graduate_friendly', 'graduate_friendly_level',
  'company_type', 'core_duty_module', 'major_require',
  'bonus_skill_cert', 'post_category', 'party_label',
  'is_synthetic', 'url_status', 'status', 'created_at', 'updated_at',
].join(', ')

// 使用
.from('job_descriptions').select(PUBLIC_JD_FIELDS)
```

**方案 B（响应层兜底）**：返回前过一遍 `sanitizeJDList`

```typescript
import { sanitizeJDList } from '@/lib/jd-sanitizer'
// ...
const cleaned = sanitizeJDList(data || [])
return NextResponse.json({ success: true, data: cleaned, ... })
```

### 双保险逻辑
- A 改完之后 select 已经不带敏感字段；B 是双保险（万一有 select(*) 漏改 / 关联查询带出）
- **两个都必须做**（不能只做一个）

### 例外
- `/api/admin/**` 一律保持原状不改（admin 需要完整字段调试）
- `/api/fix-skills-*`、`/api/check-skills-format`、`/api/add-constraints`、`/api/add-skills-constraint` 等修数据脚本（不返回数据给前端）→ 不改

### [ ] 验收（每个改的文件都要满足）
- [ ] 顶部定义了 `PUBLIC_JD_FIELDS` 常量（如果是新加的）
- [ ] `.select()` 不含 `source_url`/`source_platform`/`raw_jd`
- [ ] 响应前调用了 `sanitizeJDList` 或 `sanitizeJD`
- [ ] `pnpm run build` EXIT=0

### Commit（建议拆 2-3 个 commit）
```
feat(api): C端 JD 接口白名单 select + 脱敏中间件（方案 A+B 第一批）
feat(api): C端 JD 接口剥离敏感字段（第二批 match/search-jd）
feat(api): C端 JD 接口剥离敏感字段（第三批 interview/career-planning）
```

---

## 任务 4（P1）：写一个 e2e 验证脚本

### 文件
`scripts/verify-jd-sanitize.sh`（新建）

### 内容

```bash
#!/usr/bin/env bash
# JD 脱敏验证脚本
# 用法：./scripts/verify-jd-sanitize.sh [base_url]
# 默认 base_url=https://zhituxing.zeabur.app

set -e
BASE=${1:-https://zhituxing.zeabur.app}
SENSITIVE='source_url|source_platform|raw_jd'

declare -a ENDPOINTS=(
  "/api/jobs?page=1&pageSize=2"
  "/api/jobs/list?limit=2"
  "/api/match?limit=2"
  "/api/search-jd?keyword=运营&limit=2"
)

fail=0
for ep in "${ENDPOINTS[@]}"; do
  echo "=== $ep ==="
  resp=$(curl -s -m 15 "${BASE}${ep}")
  if echo "$resp" | grep -E "\"($SENSITIVE)\"" > /dev/null; then
    echo "❌ FAIL: 响应里发现敏感字段"
    echo "$resp" | head -c 300
    echo ""
    fail=$((fail+1))
  else
    echo "✅ PASS"
  fi
done

if [ $fail -gt 0 ]; then
  echo ""
  echo "❌ 总共 $fail 个接口未通过脱敏验证"
  exit 1
fi
echo ""
echo "🎉 全部接口脱敏验证通过"
```

### [ ] 验收
- [ ] 文件存在且 chmod +x
- [ ] 至少包含上面 4 个 endpoint
- [ ] 检测到敏感字段时 exit 1

### Commit
```
test(scripts): JD 脱敏 e2e 验证脚本
```

---

## 🏁 全部完成标准

- [ ] 任务 1：`src/lib/jd-sanitizer.ts` ✅
- [ ] 任务 2：`trae_pilot/B3_api_audit.md` ✅
- [ ] 任务 3：所有 C 端 API 改造完成且 build 通过 ✅
- [ ] 任务 4：`scripts/verify-jd-sanitize.sh` ✅
- [ ] 推 origin 后通知主控 redeploy Zeabur
- [ ] Zeabur 部署后 run `./scripts/verify-jd-sanitize.sh` 全部 PASS

预估工时：60-90 分钟（任务 3 是大头，~12 个文件改造）。
