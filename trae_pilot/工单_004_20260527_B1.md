# 第四批 Trae 工单 B1（2026-05-27）—— JD 历史数据清理三件套

> 主控：小扣 / 主人：肖赵才  
> 仓库：https://gitee.com/xiao-zhaos-legacy/zhituxing.git main  
> 前置：工单 003 已完成（commit e2edf14）  
> 来源调查：`职途星/调查报告/JD链路调查_20260527.md` + `职途星/调查报告/JD真实性抽样_20260527.md`

---

## 🎯 大背景（必读）

库里 4756 条 JD 实际分布：
- ✅ 真实 gxrc 链接：**659 条（13.9%）**，95% 可达
- ❌ `round*` 合成假数据：**2996 条**（DeepSeek 现编）
- ❌ `mock.gxrc.com` / `*.example.com` / `*.test.com`：**1101 条**（占位）

**目标**：标记假数据 + 修真实数据的 company 字段 bug + 探活下架链接。**主人决策：不物理删假数据，只加 `is_synthetic` 字段标记**（可回查、可恢复）。

---

## ⚠️ 铁律
1. **SQL 前必 grep 真实字段名**（已确认 raw_jd 字段存在于 src/app/jobs/page.tsx）
2. **每任务带 `[ ]` 验收清单**
3. **`pnpm run build` 必须 EXIT=0 才允许 push**
4. **1 任务 1 commit 1 push**
5. **不要碰** `src/lib/auth.ts` / `src/components/Navbar.tsx`
6. **不要执行任何生产 SQL 或 UPDATE**，只生成 `.sql` 文件 + Node 脚本（脚本**默认 dry-run**）
7. **不要 npm install 新依赖** —— 用已有 `@supabase/supabase-js`

---

## 任务 1（P0）：SQL 加 `is_synthetic`+`url_status` 字段 + 标记 4097 条假数据

### 文件
`sql/migrations/2026-05-27_jd_data_hygiene.sql`

### 内容要求
```sql
-- JD 数据卫生：加字段 + 标记假数据
-- 执行时间：2026-05-27
-- 执行者：人工在 Supabase Dashboard SQL Editor 执行
-- 风险：中（含 UPDATE，但只改新加字段不动业务字段）
-- ⚠️ 前置必须先备份：
--   CREATE TABLE job_descriptions_backup_20260527 AS SELECT * FROM public.job_descriptions;

BEGIN;

-- 1. 加字段
ALTER TABLE public.job_descriptions 
  ADD COLUMN IF NOT EXISTS is_synthetic BOOLEAN NOT NULL DEFAULT FALSE;
ALTER TABLE public.job_descriptions 
  ADD COLUMN IF NOT EXISTS url_status TEXT NOT NULL DEFAULT 'unknown';
-- url_status 枚举：unknown / alive / dead / synthetic

-- 2. 标记 round* 合成假数据（~2996 条）
UPDATE public.job_descriptions 
   SET is_synthetic = TRUE, url_status = 'synthetic'
 WHERE source_url LIKE 'round%';

-- 3. 标记 mock/example/test 占位假数据（~1101 条）
UPDATE public.job_descriptions 
   SET is_synthetic = TRUE, url_status = 'synthetic'
 WHERE source_url LIKE '%mock.gxrc.com%'
    OR source_url LIKE '%.example.com%'
    OR source_url LIKE '%.test.com%';

-- 4. 校验分组统计
SELECT is_synthetic, url_status, COUNT(*) AS cnt 
FROM public.job_descriptions 
GROUP BY is_synthetic, url_status ORDER BY cnt DESC;
-- 期望：synthetic=TRUE ~4097，FALSE/unknown ~659

COMMIT;
```

### [ ] 验收
- [ ] 文件存在
- [ ] 含 2 个 `ALTER TABLE ... ADD COLUMN IF NOT EXISTS`
- [ ] 含 2 个 UPDATE 分别标记 round* 和 mock/example/test
- [ ] 末尾 SELECT 校验分组
- [ ] BEGIN/COMMIT 包裹
- [ ] 顶部备份提示
- [ ] commit: `chore(db): JD 数据卫生 SQL（加 is_synthetic+url_status 标记 4097 条假数据）`

---

## 任务 2（P0）：Node 脚本回填 659 条 gxrc 的 company 字段

### 文件
`scripts/backfill-gxrc-company.ts`（参考已有 `scripts/backfill-job-fields.ts` 的连接和遍历模式）

### 背景
抽样发现 20 条 gxrc 里 **17 条 company 字段是 NULL**，但 `raw_jd` 字段（已 grep 确认存在于 src/app/jobs/page.tsx）存了完整页面文本。gxrc title 格式：`{职位名}_{公司名} - 广西人才网`

### 步骤
1. 拉 `SELECT id, job_title, company, raw_jd FROM job_descriptions WHERE source_url LIKE 'https://www.gxrc.com/%' AND (company IS NULL OR company = '')`
2. 正则从 `raw_jd` 提取 `<title>(.*?) - 广西人才网</title>` → 按 `_` 切，最后一段是公司名
3. **默认 dry-run**：只打印"将更新 N 条，示例 5 条"
4. `--apply` 才真跑 UPDATE
5. `--limit N` 支持小批测试
6. 失败记录单独打日志（id + 原因）不阻塞
7. 用已有 `@supabase/supabase-js` 和环境变量 `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`

### [ ] 验收
- [ ] 文件存在
- [ ] 默认 dry-run（不带 `--apply` 不写库）
- [ ] 正则提取 + 失败兜底
- [ ] 含 `--limit` 参数
- [ ] 没装新 npm 包
- [ ] 顶部使用示例：
  ```
  // pnpm tsx scripts/backfill-gxrc-company.ts --dry-run --limit 5
  // pnpm tsx scripts/backfill-gxrc-company.ts --apply
  ```
- [ ] commit: `feat(scripts): backfill-gxrc-company 从 raw_jd 提取公司名（默认 dry-run）`

---

## 任务 3（P1）：Node 脚本探活 659 条 gxrc 链接 + 更新 url_status

### 文件
`scripts/probe-gxrc-url-status.ts`

### 步骤
1. 拉 `SELECT id, source_url FROM job_descriptions WHERE source_url LIKE 'https://www.gxrc.com/%' AND is_synthetic = FALSE`
2. `fetch(url, { method: 'HEAD', redirect: 'follow', signal: AbortSignal.timeout(10000) })`
3. 判定：
   - 200 且最终 URL 不含 `/NoPosition/` → `alive`
   - 200 但最终 URL 含 `/NoPosition/` → `dead`（已下架兜底页）
   - 其他 → `dead`
4. **并发限制 5**（不打死 gxrc）
5. 默认 dry-run / `--apply` 才写
6. `--limit N`
7. 打印汇总：alive/dead/失败 各多少

### [ ] 验收
- [ ] 文件存在
- [ ] 默认 dry-run
- [ ] 并发限制 5
- [ ] HEAD 带 10s 超时
- [ ] NoPosition 检测逻辑
- [ ] 顶部使用示例
- [ ] commit: `feat(scripts): probe-gxrc-url-status 探活真实链接（默认 dry-run）`

---

## 任务 4（P0）：改 `src/app/api/jobs/route.ts` 让查询过滤 is_synthetic

### 背景
任务 1 SQL 跑完后库里有 `is_synthetic` 字段。**前台搜索不能再显示假数据**，否则用户点 source_url 跳转就穿帮。

### 步骤
1. grep 找出 src/ 下所有 `from('job_descriptions')`（含 admin 路径，便于全局视野）
2. **只改前台**：在 `src/app/api/jobs/route.ts` 的 `buildBaseQuery` 起点加 `.or('is_synthetic.is.null,is_synthetic.eq.false')`
3. **不要改 admin** 路径（运营要看到全部数据）
4. 如果其他 src 路径下也直接查给前端用（如 `/api/search-jd`），也加同样过滤
5. **写法用 `.or('is_synthetic.is.null,is_synthetic.eq.false')`** —— 任务 1 SQL 未跑前字段不存在时也兼容

### [ ] 验收
- [ ] grep 输出粘到 commit message
- [ ] 每个前台查询都加了过滤
- [ ] **没改 admin 路径**
- [ ] **没改 src/lib/auth.ts / src/components/Navbar.tsx**
- [ ] `pnpm run build` EXIT=0
- [ ] commit: `fix(jobs): 前台查询过滤 is_synthetic 避免显示合成假数据`

---

## 完工回报
每完成 1 个任务：`B1 任务 N 完成，commit XXXXXXX 已 push`

4 任务全完后主人按顺序执行：① SQL 备份 → ② 任务 1 SQL → ③ dry-run 任务 2 → --apply → ④ dry-run 任务 3 → --apply → ⑤ 任务 4 代码已随 Zeabur 部署。
